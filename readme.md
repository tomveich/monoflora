## Maintenance:

- `git clone (repo URL)` to clone

- `git pull --recurse-submodules` to pull in the submodules (PaperMod)
- `git submodule update --init` to clone in the submodules (PaperMod) and Update them

- `hugo server -w` to enable the development server, add `-D` to also build drafts 